{
  "name": "coverage-test-service",
  "version": "0.1.0",
  "description": "Node.js backend service listening over MQTT and plotting into Google Sheets",
  "main": "index.js",
  "scripts": {
    "start": "node client.js -u <username> -p <password> -t 'thing-update/<domain>/<thing ID>' -s '<sheet ID>'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pontus Aurdal",
  "license": "ISC",
  "dependencies": {
    "aws-iot-device-sdk": "^1.0.14",
    "aws-sdk": "^2.28.0",
    "google-spreadsheet": "^2.0.4",
    "mqtt": "^2.4.0",
    "optimist": "^0.6.1",
    "ora": "^1.1.0",
    "request": "^2.81.0",
    "winston": "^2.3.1"
  }
}
